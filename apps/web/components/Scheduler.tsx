import React from 'react'

const Scheduler = ({data}:{data:any}) => {
    const {setAddNewNodeModal,hasChild} = data;
  return (
    <div className='relative flex'>
      
    </div>
  )
}

export default Scheduler
